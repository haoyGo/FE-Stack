### Q9: 设计前端监控系统

**考察点：** 系统设计、全链路监控、故障排查

#### 完整方案

**1. 监控维度**

```javascript
// 性能监控
const performanceMetrics = {
  // Web Vitals
  lcp: "最大内容绘制",
  fid: "首次输入延迟",
  cls: "累积布局偏移",

  // 自定义指标
  tti: "可交互时间",
  fcp: "首次内容绘制",
  apiDuration: "API 响应时间",
  resourceLoadTime: "资源加载时间",
};

// 错误监控
const errorMetrics = {
  jsError: "JavaScript 错误",
  apiError: "API 请求失败",
  resourceError: "资源加载失败",
  promiseRejection: "Promise 未捕获异常",
};

// 用户行为监控
const behaviorMetrics = {
  pv: "页面浏览量",
  uv: "独立访客数",
  sessionDuration: "会话时长",
  clickHeatmap: "点击热力图",
};
```

**2. SDK 设计**

```javascript
// monitor-sdk.js
class MonitorSDK {
  constructor(config) {
    this.config = config;
    this.queue = [];
    this.init();
  }

  init() {
    this.listenErrors();
    this.listenPerformance();
    this.listenUserBehavior();
    this.startHeartbeat();
  }

  // 错误监控
  listenErrors() {
    // JS 错误
    window.addEventListener("error", (event) => {
      this.report({
        type: "jsError",
        message: event.message,
        stack: event.error?.stack,
        filename: event.filename,
        lineno: event.lineno,
        colno: event.colno,
      });
    });

    // Promise 异常
    window.addEventListener("unhandledrejection", (event) => {
      this.report({
        type: "promiseRejection",
        reason: event.reason,
      });
    });

    // 资源加载失败
    window.addEventListener(
      "error",
      (event) => {
        if (event.target !== window) {
          this.report({
            type: "resourceError",
            url: event.target.src || event.target.href,
            tagName: event.target.tagName,
          });
        }
      },
      true
    );
  }

  // 性能监控
  listenPerformance() {
    // Web Vitals
    import("web-vitals").then(({ getCLS, getFID, getLCP }) => {
      getCLS(this.reportWebVitals.bind(this));
      getFID(this.reportWebVitals.bind(this));
      getLCP(this.reportWebVitals.bind(this));
    });

    // Navigation Timing
    window.addEventListener("load", () => {
      const timing = performance.getEntriesByType("navigation")[0];
      this.report({
        type: "performance",
        dnsTime: timing.domainLookupEnd - timing.domainLookupStart,
        tcpTime: timing.connectEnd - timing.connectStart,
        ttfb: timing.responseStart - timing.requestStart,
        domParse: timing.domInteractive - timing.responseEnd,
        domReady: timing.domContentLoadedEventEnd - timing.fetchStart,
        loadComplete: timing.loadEventEnd - timing.fetchStart,
      });
    });
  }

  // 用户行为监控
  listenUserBehavior() {
    // PV 统计
    this.report({
      type: "pv",
      url: location.href,
      referrer: document.referrer,
    });

    // 点击事件
    document.addEventListener("click", (event) => {
      const target = event.target;
      this.report({
        type: "click",
        xpath: this.getXPath(target),
        text: target.innerText?.slice(0, 100),
      });
    });
  }

  // 心跳检测
  startHeartbeat() {
    setInterval(() => {
      this.report({
        type: "heartbeat",
        timestamp: Date.now(),
      });
    }, 60000); // 1 分钟
  }

  // 上报
  report(data) {
    this.queue.push({
      ...data,
      userId: this.getUserId(),
      sessionId: this.getSessionId(),
      timestamp: Date.now(),
      ua: navigator.userAgent,
      url: location.href,
    });

    // 批量发送
    if (this.queue.length >= 10) {
      this.flush();
    }
  }

  flush() {
    if (this.queue.length === 0) return;

    const data = this.queue.splice(0);

    // 使用 sendBeacon（页面卸载时不丢数据）
    navigator.sendBeacon(this.config.reportUrl, JSON.stringify(data));
  }

  getXPath(element) {
    if (element.id) return `//*[@id="${element.id}"]`;

    const paths = [];
    for (; element && element.nodeType === 1; element = element.parentNode) {
      let index = 0;
      for (
        let sibling = element.previousSibling;
        sibling;
        sibling = sibling.previousSibling
      ) {
        if (
          sibling.nodeType === Node.ELEMENT_NODE &&
          sibling.tagName === element.tagName
        ) {
          index++;
        }
      }
      const tagName = element.tagName.toLowerCase();
      const pathIndex = index ? `[${index + 1}]` : "";
      paths.unshift(`${tagName}${pathIndex}`);
    }
    return `/${paths.join("/")}`;
  }
}

// 使用
const monitor = new MonitorSDK({
  reportUrl: "https://monitor.xiaohongshu.com/api/report",
  sampleRate: 0.1, // 10% 采样
});
```

**3. 数据分析平台**

```javascript
// 数据聚合（后端）
// 1. 实时流处理（Flink）
// 2. 时序数据库（InfluxDB）
// 3. 数据可视化（Grafana）

// 告警规则
const alertRules = [
  {
    name: "LCP 性能告警",
    condition: "lcp > 4000", // LCP > 4 秒
    severity: "high",
    notifyChannels: ["email", "feishu"],
  },
  {
    name: "JS 错误率告警",
    condition: "errorRate > 1%",
    severity: "critical",
    notifyChannels: ["phone", "feishu"],
  },
];
```

---
